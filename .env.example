# Copy to .env and fill in values

# API key shared across all 3 services (llm, embedding, reranker)
API_KEY=sk-secai2026

# HuggingFace token (required if models are gated)
HF_TOKEN=hf_xxx

# SXM A100 has NVLink — P2P stays enabled by default.
# Uncomment only if running on PCIe A100 without NVLink:
# NCCL_P2P_DISABLE=1
